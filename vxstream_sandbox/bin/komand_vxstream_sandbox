#!/usr/bin/env python
# GENERATED BY KOMAND SDK - DO NOT EDIT
import komand
from komand_vxstream_sandbox import connection, actions, triggers


Name = 'VxStream Sandbox'
Vendor = 'rapid7'
Version = '2.0.1'
Description = 'Submit files for analysis and view file report details. Search the service for file hashes of malware'


class ICONVxstreamSandbox(komand.Plugin):
    def __init__(self):
        super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
                )
        self.add_action(actions.Lookup())

        self.add_action(actions.Query())

        self.add_action(actions.Report())

        self.add_action(actions.Submit())


def main():
    """Run plugin"""
    cli = komand.CLI(ICONVxstreamSandbox())
    cli.run()


if __name__ == "__main__":
    main()
