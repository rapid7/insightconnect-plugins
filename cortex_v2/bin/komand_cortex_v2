#!/usr/bin/env python
# GENERATED BY KOMAND SDK - DO NOT EDIT
import komand
from komand_cortex_v2 import connection, actions, triggers


Name = 'Cortex v2'
Vendor = 'rapid7'
Version = '1.1.0'
Description = 'Cortex, an open source and free software for analyzing observables'


class ICONCortexV2(komand.Plugin):
    def __init__(self):
        super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
                )
        self.add_action(actions.BulkAnalyze())

        self.add_action(actions.DeleteJob())

        self.add_action(actions.GetAnalyzer())

        self.add_action(actions.GetAnalyzerByType())

        self.add_action(actions.GetJobDetails())

        self.add_action(actions.GetJobReport())

        self.add_action(actions.GetJobs())

        self.add_action(actions.RunAnalyzer())

        self.add_action(actions.RunFileAnalyzer())


def main():
    """Run plugin"""
    cli = komand.CLI(ICONCortexV2())
    cli.run()


if __name__ == "__main__":
    main()
