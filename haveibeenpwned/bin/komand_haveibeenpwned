#!/usr/bin/env python
# GENERATED BY KOMAND SDK - DO NOT EDIT
import komand
from komand_haveibeenpwned import connection, actions, triggers


Name = 'HaveIBeenPwned'
Vendor = 'rapid7'
Version = '3.0.1'
Description = 'Have I been pwned? allows you to search across multiple data breaches to see if your email addresses has been compromised'


class ICONHaveibeenpwned(komand.Plugin):
    def __init__(self):
        super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
                )
        self.add_action(actions.LookupDomain())

        self.add_action(actions.LookupPassword())

        self.add_action(actions.LookupUser())


def main():
    """Run plugin"""
    cli = komand.CLI(ICONHaveibeenpwned())
    cli.run()


if __name__ == "__main__":
    main()
