# GENERATED BY KOMAND SDK - DO NOT EDIT
import komand
import json


class Component:
    DESCRIPTION = "Reload an agent module (applies to Windows agents only)"


class Input:
    FILTER = "filter"
    MODULE = "module"
    

class Output:
    AFFECTED = "affected"
    

class AgentsReloadInput(komand.Input):
    schema = json.loads("""
   {
  "type": "object",
  "title": "Variables",
  "properties": {
    "filter": {
      "type": "object",
      "title": "Filter JSON",
      "description": "Applied filter - only matched agents will be affected by the requested action. Leave empty to apply the action on all applicable agents",
      "order": 1
    },
    "module": {
      "type": "string",
      "title": "Data Module",
      "description": "Agent module to reload",
      "enum": [
        "monitor",
        "static",
        "agent",
        "log"
      ],
      "order": 2
    }
  },
  "required": [
    "filter",
    "module"
  ]
}
    """)

    def __init__(self):
        super(self.__class__, self).__init__(self.schema)


class AgentsReloadOutput(komand.Output):
    schema = json.loads("""
   {
  "type": "object",
  "title": "Variables",
  "properties": {
    "affected": {
      "type": "integer",
      "title": "Affected",
      "description": "Number of entities affected by the requested operation",
      "order": 1
    }
  }
}
    """)

    def __init__(self):
        super(self.__class__, self).__init__(self.schema)
