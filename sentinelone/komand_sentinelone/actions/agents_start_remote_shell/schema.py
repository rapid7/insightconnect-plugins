# GENERATED BY KOMAND SDK - DO NOT EDIT
import komand
import json


class Component:
    DESCRIPTION = "Start a remote shell if the 2FA code is valid the history password will be used to compress the shell history and rows/columns to configure the remote shell to the size UI is using"


class Input:
    COLUMNS = "columns"
    FILTER = "filter"
    HISTORY_PASSWORD = "history_password"
    ROWS = "rows"
    TWO_FA_CODE = "two_fa_code"
    

class Output:
    AFFECTED = "affected"
    

class AgentsStartRemoteShellInput(komand.Input):
    schema = json.loads("""
   {
  "type": "object",
  "title": "Variables",
  "properties": {
    "columns": {
      "type": "integer",
      "title": "Columns Number",
      "description": "Number of columns of the console shell",
      "order": 2
    },
    "filter": {
      "type": "object",
      "title": "Filter JSON",
      "description": "Applied filter - only matched agents will be affected by the requested action. Leave empty to apply the action on all applicable agents",
      "order": 1
    },
    "history_password": {
      "type": "string",
      "title": "History Password",
      "description": "Password to zip the shell history file at end of session",
      "order": 4
    },
    "rows": {
      "type": "integer",
      "title": "Rows",
      "description": "Number of rows of the console shell",
      "order": 5
    },
    "two_fa_code": {
      "type": "string",
      "title": "2FA Code",
      "description": "The 2FA code to authenticate the user",
      "order": 3
    }
  },
  "required": [
    "columns",
    "filter",
    "history_password",
    "rows",
    "two_fa_code"
  ]
}
    """)

    def __init__(self):
        super(self.__class__, self).__init__(self.schema)


class AgentsStartRemoteShellOutput(komand.Output):
    schema = json.loads("""
   {
  "type": "object",
  "title": "Variables",
  "properties": {
    "affected": {
      "type": "integer",
      "title": "Affected",
      "description": "Number of entities affected by the requested operation",
      "order": 1
    }
  }
}
    """)

    def __init__(self):
        super(self.__class__, self).__init__(self.schema)
