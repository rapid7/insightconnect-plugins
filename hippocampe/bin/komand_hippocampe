#!/usr/bin/env python
# GENERATED BY KOMAND SDK - DO NOT EDIT
import komand
from komand_hippocampe import connection, actions, triggers


Name = 'Hippocampe'
Vendor = 'rapid7'
Version = '1.0.0'
Description = 'The Hippocampe plugin allows for advanced queries and management of your Hippocampe feeds'


class ICONHippocampe(komand.Plugin):
    def __init__(self):
        super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
                )
        self.add_action(actions.Distinct())

        self.add_action(actions.Freshness())

        self.add_action(actions.Hipposched())

        self.add_action(actions.Hipposcore())

        self.add_action(actions.Jobs())

        self.add_action(actions.LastQuery())

        self.add_action(actions.LastStatus())

        self.add_action(actions.MonitorSources())

        self.add_action(actions.More())

        self.add_action(actions.New())

        self.add_action(actions.SchedReport())

        self.add_action(actions.Shadowbook())

        self.add_action(actions.SizeBySource())

        self.add_action(actions.SizeByType())

        self.add_action(actions.Sources())

        self.add_action(actions.Type())


def main():
    """Run plugin"""
    cli = komand.CLI(ICONHippocampe())
    cli.run()


if __name__ == "__main__":
    main()
