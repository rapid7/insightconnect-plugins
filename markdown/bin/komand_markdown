#!/usr/bin/env python
# GENERATED BY KOMAND SDK - DO NOT EDIT
import komand
from komand_markdown import connection, actions, triggers


Name = 'Markdown'
Vendor = 'rapid7'
Version = '2.2.1'
Description = 'Convert markdown to and from various popular formats'


class ICONMarkdown(komand.Plugin):
    def __init__(self):
        super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
                )
        self.add_action(actions.HtmlToMarkdown())

        self.add_action(actions.MarkdownToHtml())

        self.add_action(actions.MarkdownToPdf())


def main():
    """Run plugin"""
    cli = komand.CLI(ICONMarkdown())
    cli.run()


if __name__ == "__main__":
    main()
