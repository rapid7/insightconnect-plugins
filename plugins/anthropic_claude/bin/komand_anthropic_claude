#!/usr/bin/env python
# GENERATED BY INSIGHT-PLUGIN - DO NOT EDIT
import os
import json
from sys import argv

Name = "Anthropic Claude"
Vendor = "rapid7_ew"
Version = "1.0.0"
Description = "Leverage Anthropic's Claude AI models to assist with security analysis, incident summarization, IOC analysis, and natural language processing tasks in SOC workflows"


def main():
    if 'http' in argv:
        if os.environ.get("GUNICORN_CONFIG_FILE"):
            with open(os.environ.get("GUNICORN_CONFIG_FILE")) as gf:
                gunicorn_cfg = json.load(gf)
                if gunicorn_cfg.get("worker_class", "sync") == "gevent":
                    from gevent import monkey
                    monkey.patch_all()
        elif 'gevent' in argv:
            from gevent import monkey
            monkey.patch_all()

    import insightconnect_plugin_runtime
    from komand_anthropic_claude import connection, actions, triggers, tasks

    class ICONAnthropicClaude(insightconnect_plugin_runtime.Plugin):
        def __init__(self):
            super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
            )
            self.add_action(actions.SendPrompt())
        
            self.add_action(actions.AnalyzeIocs())
        
            self.add_action(actions.SummarizeText())
        
            self.add_action(actions.CountTokens())
        

    """Run plugin"""
    cli = insightconnect_plugin_runtime.CLI(ICONAnthropicClaude())
    cli.run()


if __name__ == "__main__":
    main()
