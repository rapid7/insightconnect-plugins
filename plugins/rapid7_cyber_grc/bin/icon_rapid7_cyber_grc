#!/usr/bin/env python
# GENERATED BY INSIGHT-PLUGIN - DO NOT EDIT
import os
import json
from sys import argv

Name = "Rapid7 Cyber GRC"
Vendor = "rapid7"
Version = "1.0.0"
Description = "Rapid7 Cyber GRC is a governance, risk, and compliance solution. This plugin manages risks, incidents, tasks, audits, control sets, assessments, vendors, certifications, IT assets, and users, and uploads evidence through the Flat File API"


def main():
    if 'http' in argv:
        if os.environ.get("GUNICORN_CONFIG_FILE"):
            with open(os.environ.get("GUNICORN_CONFIG_FILE")) as gf:
                gunicorn_cfg = json.load(gf)
                if gunicorn_cfg.get("worker_class", "sync") == "gevent":
                    from gevent import monkey
                    monkey.patch_all()
        elif 'gevent' in argv:
            from gevent import monkey
            monkey.patch_all()

    import insightconnect_plugin_runtime
    from icon_rapid7_cyber_grc import connection, actions, triggers, tasks

    class ICONRapid7CyberGrc(insightconnect_plugin_runtime.Plugin):
        def __init__(self):
            super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
            )
            self.add_trigger(triggers.MonitorRecords())
        
            self.add_trigger(triggers.MonitorComplianceDrift())
        
            self.add_action(actions.GetRisks())
        
            self.add_action(actions.GetRisk())
        
            self.add_action(actions.CreateRisk())
        
            self.add_action(actions.UpdateRisk())
        
            self.add_action(actions.DeleteRisk())
        
            self.add_action(actions.GetIncidents())
        
            self.add_action(actions.GetIncident())
        
            self.add_action(actions.CreateIncident())
        
            self.add_action(actions.UpdateIncident())
        
            self.add_action(actions.DeleteIncident())
        
            self.add_action(actions.GetTasks())
        
            self.add_action(actions.GetTask())
        
            self.add_action(actions.CreateTask())
        
            self.add_action(actions.UpdateTask())
        
            self.add_action(actions.DeleteTask())
        
            self.add_action(actions.AddComment())
        
            self.add_action(actions.GetAudits())
        
            self.add_action(actions.GetAudit())
        
            self.add_action(actions.CreateAudit())
        
            self.add_action(actions.UpdateAudit())
        
            self.add_action(actions.DeleteAudit())
        
            self.add_action(actions.GetControlSets())
        
            self.add_action(actions.GetControlSet())
        
            self.add_action(actions.CreateControlSet())
        
            self.add_action(actions.UpdateControlSet())
        
            self.add_action(actions.DeleteControlSet())
        
            self.add_action(actions.GetComplianceScore())
        
            self.add_action(actions.GetAssessments())
        
            self.add_action(actions.GetAssessment())
        
            self.add_action(actions.CreateAssessment())
        
            self.add_action(actions.UpdateAssessment())
        
            self.add_action(actions.DeleteAssessment())
        
            self.add_action(actions.GetVendors())
        
            self.add_action(actions.GetVendor())
        
            self.add_action(actions.CreateVendor())
        
            self.add_action(actions.UpdateVendor())
        
            self.add_action(actions.DeleteVendor())
        
            self.add_action(actions.GetContracts())
        
            self.add_action(actions.GetContract())
        
            self.add_action(actions.CreateContract())
        
            self.add_action(actions.UpdateContract())
        
            self.add_action(actions.DeleteContract())
        
            self.add_action(actions.GetCertifications())
        
            self.add_action(actions.GetCertification())
        
            self.add_action(actions.CreateCertification())
        
            self.add_action(actions.UpdateCertification())
        
            self.add_action(actions.DeleteCertification())
        
            self.add_action(actions.GetItAssets())
        
            self.add_action(actions.GetItAsset())
        
            self.add_action(actions.CreateItAsset())
        
            self.add_action(actions.UpdateItAsset())
        
            self.add_action(actions.DeleteItAsset())
        
            self.add_action(actions.GetUsers())
        
            self.add_action(actions.GetUser())
        
            self.add_action(actions.CreateUser())
        
            self.add_action(actions.UpdateUser())
        
            self.add_action(actions.DeleteUser())
        
            self.add_action(actions.ListRecords())
        
            self.add_action(actions.GetRecord())
        
            self.add_action(actions.CreateRecord())
        
            self.add_action(actions.UpdateRecord())
        
            self.add_action(actions.DeleteRecord())
        
            self.add_action(actions.CountRecords())
        
            self.add_action(actions.GetRecordHistory())
        
            self.add_action(actions.UploadFlatFile())
        

    """Run plugin"""
    cli = insightconnect_plugin_runtime.CLI(ICONRapid7CyberGrc())
    cli.run()


if __name__ == "__main__":
    main()
