# GENERATED BY INSIGHT-PLUGIN - DO NOT EDIT
import insightconnect_plugin_runtime
import json


class Component:
    DESCRIPTION = "Takes a screenshot of a URL from the Orchestrator"


class Input:
    DELAY = "delay"
    FULL_PAGE = "full_page"
    URL = "url"


class Output:
    SCREENSHOT = "screenshot"


class ScreenshotInput(insightconnect_plugin_runtime.Input):
    schema = json.loads(r"""
   {
  "type": "object",
  "title": "Variables",
  "properties": {
    "delay": {
      "type": "integer",
      "title": "Delay",
      "description": "Delay (in seconds) after page loads before taking a screenshot",
      "order": 2
    },
    "full_page": {
      "type": "boolean",
      "title": "Full Page Capture",
      "description": "Export screenshot of `body` element instead of the default viewport. This `body` element may have some formatting issues but should contain all elements of the page",
      "default": false,
      "order": 3
    },
    "url": {
      "type": "string",
      "title": "URL",
      "description": "The URL to take a screenshot from",
      "order": 1
    }
  },
  "required": [
    "full_page",
    "url"
  ],
  "definitions": {}
}
    """)

    def __init__(self):
        super(self.__class__, self).__init__(self.schema)


class ScreenshotOutput(insightconnect_plugin_runtime.Output):
    schema = json.loads(r"""
   {
  "type": "object",
  "title": "Variables",
  "properties": {
    "screenshot": {
      "type": "string",
      "format": "bytes",
      "displayType": "bytes",
      "title": "Screenshot",
      "description": "The resulting PNG screenshot of the URL",
      "order": 1
    }
  },
  "definitions": {}
}
    """)

    def __init__(self):
        super(self.__class__, self).__init__(self.schema)
