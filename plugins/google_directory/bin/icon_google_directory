#!/usr/bin/env python
# GENERATED BY KOMAND SDK - DO NOT EDIT
import os
import json
from sys import argv

Name = "Google Directory"
Vendor = "rapid7"
Version = "2.0.2"
Description = "Uses the Google Directory plugin to manage members, users, and user aliases"


def main():
    if 'http' in argv:
        if os.environ.get("GUNICORN_CONFIG_FILE"):
            with open(os.environ.get("GUNICORN_CONFIG_FILE")) as gf:
                gunicorn_cfg = json.load(gf)
                if gunicorn_cfg.get("worker_class", "sync") == "gevent":
                    from gevent import monkey
                    monkey.patch_all()
        elif 'gevent' in argv:
            from gevent import monkey
            monkey.patch_all()

    import insightconnect_plugin_runtime
    from icon_google_directory import connection, actions, triggers

    class ICONGoogleDirectory(insightconnect_plugin_runtime.Plugin):
        def __init__(self):
            super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
            )
            self.add_action(actions.GetAllDomainUsers())

            self.add_action(actions.SuspendUser())

            self.add_action(actions.UnsuspendUser())


    """Run plugin"""
    cli = insightconnect_plugin_runtime.CLI(ICONGoogleDirectory())
    cli.run()


if __name__ == "__main__":
    main()
