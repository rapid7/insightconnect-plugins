#!/usr/bin/env python
# GENERATED BY INSIGHT-PLUGIN - DO NOT EDIT
import os
import json
from sys import argv

Name = "Okta"
Vendor = "rapid7"
Version = "4.2.14"
Description = "[Okta](https://www.okta.com/) is a SSO and account lifecycle management provider that allows companies to integrate their central user account system with a wide variety of other applications and services"


def main():
    if 'http' in argv:
        if os.environ.get("GUNICORN_CONFIG_FILE"):
            with open(os.environ.get("GUNICORN_CONFIG_FILE")) as gf:
                gunicorn_cfg = json.load(gf)
                if gunicorn_cfg.get("worker_class", "sync") == "gevent":
                    from gevent import monkey
                    monkey.patch_all()
        elif 'gevent' in argv:
            from gevent import monkey
            monkey.patch_all()

    import insightconnect_plugin_runtime
    from komand_okta import connection, actions, triggers, tasks

    class ICONOkta(insightconnect_plugin_runtime.Plugin):
        def __init__(self):
            super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
            )
            self.add_trigger(triggers.UsersAddedRemovedFromGroup())
        
            self.add_action(actions.GetFactors())
        
            self.add_action(actions.SendPush())
        
            self.add_action(actions.DeactivateUser())
        
            self.add_action(actions.DeleteUser())
        
            self.add_action(actions.SuspendUser())
        
            self.add_action(actions.UnsuspendUser())
        
            self.add_action(actions.GetUser())
        
            self.add_action(actions.AddUserToGroup())
        
            self.add_action(actions.RemoveUserFromGroup())
        
            self.add_action(actions.ListGroups())
        
            self.add_action(actions.AssignUserToAppSso())
        
            self.add_action(actions.CreateUser())
        
            self.add_action(actions.ResetFactors())
        
            self.add_action(actions.ResetPassword())
        
            self.add_action(actions.UpdateBlacklistZones())
        
            self.add_action(actions.GetUserGroups())
        
            self.add_task(tasks.MonitorLogs())
        

    """Run plugin"""
    cli = insightconnect_plugin_runtime.CLI(ICONOkta())
    cli.run()


if __name__ == "__main__":
    main()
