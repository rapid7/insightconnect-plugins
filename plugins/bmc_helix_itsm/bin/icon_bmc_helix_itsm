#!/usr/bin/env python
# GENERATED BY KOMAND SDK - DO NOT EDIT
import os
import json
from sys import argv

Name = "BMC Helix ITSM"
Vendor = "rapid7"
Version = "1.0.0"
Description = "BMC Helix IT Service Management is a cloud-native SaaS solution leveraging AI and automation to accelerate IT team results"


def main():
    if 'http' in argv:
        if os.environ.get("GUNICORN_CONFIG_FILE"):
            with open(os.environ.get("GUNICORN_CONFIG_FILE")) as gf:
                gunicorn_cfg = json.load(gf)
                if gunicorn_cfg.get("worker_class", "sync") == "gevent":
                    from gevent import monkey
                    monkey.patch_all()
        elif 'gevent' in argv:
            from gevent import monkey
            monkey.patch_all()

    import insightconnect_plugin_runtime
    from icon_bmc_helix_itsm import connection, actions, triggers

    class ICONBmcHelixItsm(insightconnect_plugin_runtime.Plugin):
        def __init__(self):
            super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
            )
            self.add_action(actions.CreateIncident())

            self.add_action(actions.CreateProblemInvestigation())

            self.add_action(actions.CreateTask())

            self.add_action(actions.GetIncident())

            self.add_action(actions.GetIncidentWorkInformation())

            self.add_action(actions.ModifyIncident())


    """Run plugin"""
    cli = insightconnect_plugin_runtime.CLI(ICONBmcHelixItsm())
    cli.run()


if __name__ == "__main__":
    main()
