#!/usr/bin/env python
# GENERATED BY INSIGHT-PLUGIN - DO NOT EDIT
import os
import json
from sys import argv

Name = "Joe Sandbox"
Vendor = "rapid7"
Version = "3.0.0"
Description = "Joe Sandbox Cloud executes files and URLs fully automated in a controlled environment and monitors the behavior of applications and the operating system for suspicious activities"


def main():
    if 'http' in argv:
        if os.environ.get("GUNICORN_CONFIG_FILE"):
            with open(os.environ.get("GUNICORN_CONFIG_FILE")) as gf:
                gunicorn_cfg = json.load(gf)
                if gunicorn_cfg.get("worker_class", "sync") == "gevent":
                    from gevent import monkey
                    monkey.patch_all()
        elif 'gevent' in argv:
            from gevent import monkey
            monkey.patch_all()

    import insightconnect_plugin_runtime
    from icon_joe_sandbox import connection, actions, triggers, tasks

    class ICONJoeSandbox(insightconnect_plugin_runtime.Plugin):
        def __init__(self):
            super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
            )
            self.add_action(actions.CheckServerStatus())
        
            self.add_action(actions.GetServerInfo())
        
            self.add_action(actions.ListLanguagesAndLocales())
        
            self.add_action(actions.ListSystems())
        
            self.add_action(actions.ListCountries())
        
            self.add_action(actions.GetAccountInfo())
        
            self.add_action(actions.SubmitSample())
        
            self.add_action(actions.SubmitSampleUrl())
        
            self.add_action(actions.SubmitUrl())
        
            self.add_action(actions.SubmitCookbook())
        
            self.add_action(actions.ListAnalyses())
        
            self.add_action(actions.GetAnalysisInfo())
        
            self.add_action(actions.DeleteAnalysis())
        
            self.add_action(actions.SearchAnalysis())
        
            self.add_action(actions.DownloadAnalysis())
        
            self.add_action(actions.GetSubmittedInfo())
        

    """Run plugin"""
    cli = insightconnect_plugin_runtime.CLI(ICONJoeSandbox())
    cli.run()


if __name__ == "__main__":
    main()
