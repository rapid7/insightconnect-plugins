#!/usr/bin/env python
# GENERATED BY INSIGHT-PLUGIN - DO NOT EDIT
import os
import json
from sys import argv

Name = "ANY.RUN"
Vendor = "rapid7"
Version = "3.0.0"
Description = "ANY.RUN Sandbox is an online interactive sandbox for malware analysis, a tool for detection, monitoring, and research of cyber threats in real time"


def main():
    if 'http' in argv:
        if os.environ.get("GUNICORN_CONFIG_FILE"):
            with open(os.environ.get("GUNICORN_CONFIG_FILE")) as gf:
                gunicorn_cfg = json.load(gf)
                if gunicorn_cfg.get("worker_class", "sync") == "gevent":
                    from gevent import monkey
                    monkey.patch_all()
        elif 'gevent' in argv:
            from gevent import monkey
            monkey.patch_all()

    import insightconnect_plugin_runtime
    from icon_any_run import connection, actions, triggers, tasks

    class ICONAnyRun(insightconnect_plugin_runtime.Plugin):
        def __init__(self):
            super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
            )
            self.add_action(actions.GetAnalysisHistory())
        
            self.add_action(actions.GetAnalysisReport())
        
            self.add_action(actions.GetAnalysisVerdict())
        
            self.add_action(actions.GetIoc())
        
            self.add_action(actions.DownloadPcap())
        
            self.add_action(actions.WindowsUrlAnalysis())
        
            self.add_action(actions.LinuxUrlAnalysis())
        
            self.add_action(actions.AndroidUrlAnalysis())
        
            self.add_action(actions.WindowsFileAnalysis())
        
            self.add_action(actions.LinuxFileAnalysis())
        
            self.add_action(actions.AndroidFileAnalysis())
        
            self.add_action(actions.GetIntelligence())
        
            self.add_action(actions.GetReputation())
        

    """Run plugin"""
    cli = insightconnect_plugin_runtime.CLI(ICONAnyRun())
    cli.run()


if __name__ == "__main__":
    main()
