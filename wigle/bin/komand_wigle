#!/usr/bin/env python
# GENERATED BY KOMAND SDK - DO NOT EDIT
import komand
from komand_wigle import connection, actions, triggers


Name = 'WiGLE'
Vendor = 'rapid7'
Version = '1.0.1'
Description = 'The WiGLE (Wireless Geographic Logging Engine) plugin will get and send information about wireless networks'


class ICONWigle(komand.Plugin):
    def __init__(self):
        super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
                )
        self.add_action(actions.AddComment())

        self.add_action(actions.GetCountryStatistics())

        self.add_action(actions.GetFilesStatus())

        self.add_action(actions.GetGeneralStatistics())

        self.add_action(actions.GetGroupStatistics())

        self.add_action(actions.GetKml())

        self.add_action(actions.GetMetadata())

        self.add_action(actions.GetNetworkDetails())

        self.add_action(actions.GetNetworkGeocode())

        self.add_action(actions.GetRegionStatistics())

        self.add_action(actions.GetSiteStatistics())

        self.add_action(actions.GetUserProfile())

        self.add_action(actions.GetUserStandings())

        self.add_action(actions.GetUserStatistics())

        self.add_action(actions.GetUserTokens())

        self.add_action(actions.SearchCells())

        self.add_action(actions.SearchNetworks())

        self.add_action(actions.UploadFile())


def main():
    """Run plugin"""
    cli = komand.CLI(ICONWigle())
    cli.run()


if __name__ == "__main__":
    main()
