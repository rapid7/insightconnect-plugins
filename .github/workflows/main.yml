name: PluginValidator

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Validate Plugin(s)
      run: |
        python3 -m pip install --upgrade pip
        curl -s https://<token here>@packagecloud.io/install/repositories/rapid7/insightconnect_internal_python_tooling/script.python.sh | bash
        python3 -m pip install icon-integrations-ci
        
