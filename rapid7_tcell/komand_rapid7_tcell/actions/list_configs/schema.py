# GENERATED BY KOMAND SDK - DO NOT EDIT
import komand
import json


class Input:
    APP_ID = "app_id"
    FROM = "from"
    PAGE = "page"
    PER_PAGE = "per_page"
    TO = "to"
    

class Output:
    CONFIGS = "configs"
    TOTAL = "total"
    

class ListConfigsInput(komand.Input):
    schema = json.loads("""
   {
  "type": "object",
  "title": "Variables",
  "properties": {
    "app_id": {
      "type": "string",
      "title": "App ID",
      "description": "App ID",
      "order": 1
    },
    "from": {
      "type": "integer",
      "title": "From",
      "description": "Unix timestamp (in milliseconds; exclusive) from which to fetch events",
      "order": 2
    },
    "page": {
      "type": "integer",
      "title": "Page",
      "description": "Select which page is returned (defaults to 1)",
      "default": 1,
      "order": 5
    },
    "per_page": {
      "type": "integer",
      "title": "Per Page",
      "description": "The number of results to be returned per page",
      "default": 10,
      "order": 4
    },
    "to": {
      "type": "integer",
      "title": "To",
      "description": "Unix timestamp (in milliseconds; inclusive) until which to fetch events",
      "order": 3
    }
  },
  "required": [
    "app_id"
  ]
}
    """)

    def __init__(self):
        super(self.__class__, self).__init__(self.schema)


class ListConfigsOutput(komand.Output):
    schema = json.loads("""
   {
  "type": "object",
  "title": "Variables",
  "properties": {
    "configs": {
      "type": "array",
      "title": "Configurations",
      "description": "A list of configurations matching the provided criteria",
      "items": {
        "type": "object"
      },
      "order": 1
    },
    "total": {
      "type": "integer",
      "title": "Total",
      "description": "The number of items returned",
      "order": 2
    }
  }
}
    """)

    def __init__(self):
        super(self.__class__, self).__init__(self.schema)
