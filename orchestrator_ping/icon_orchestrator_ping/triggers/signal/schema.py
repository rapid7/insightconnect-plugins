# GENERATED BY KOMAND SDK - DO NOT EDIT
import komand
import json


class Component:
    DESCRIPTION = "Send a signal to Insight Connect during startup and shutdown"


class Input:
    
    SHUTDOWN_MESSAGE = "shutdown_message"
    STARTUP_MESSAGE = "startup_message"
    

class Output:
    
    MESSAGE = "message"
    

class SignalInput(komand.Input):
    schema = json.loads("""
   {
  "type": "object",
  "title": "Variables",
  "properties": {
    "shutdown_message": {
      "type": "string",
      "title": "Shutdown Message",
      "description": "The message to return to Insight Connect when the Orchestrator shuts down",
      "order": 2
    },
    "startup_message": {
      "type": "string",
      "title": "Startup Message",
      "description": "The message to return to Insight Connect when the Orchestrator starts the trigger",
      "order": 1
    }
  },
  "required": [
    "shutdown_message",
    "startup_message"
  ]
}
    """)

    def __init__(self):
        super(self.__class__, self).__init__(self.schema)


class SignalOutput(komand.Output):
    schema = json.loads("""
   {
  "type": "object",
  "title": "Variables",
  "properties": {
    "message": {
      "type": "string",
      "title": "Message",
      "description": "The message with the signal, either Startup or Shutdown depending on the event",
      "order": 1
    }
  }
}
    """)

    def __init__(self):
        super(self.__class__, self).__init__(self.schema)
