package main

// Code generated by the Komand Go SDK Generator. DO NOT EDIT

import (
	"context"
	"encoding/json"
	"fmt"
	"log"
	"os"
	"os/signal"
	"strings"
	"syscall"

	"github.com/rapid7/komand-plugin-sdk-go2/cli"
	plog "github.com/rapid7/komand-plugin-sdk-go2/log"
	"github.com/rapid7/komand-plugin-sdk-go2/message"
	"github.com/rapid7/komand-plugins/type_converter/actions"
	"github.com/rapid7/komand-plugins/type_converter/connection"
	"github.com/rapid7/komand-plugins/type_converter/server/http"
	"gopkg.in/alecthomas/kingpin.v2"
)

var (
	// Name plugin name
	Name = "type_converter"
	// Vendor plugin vendor
	Vendor = "rapid7"
	// Version plugin version
	Version = "1.5.0"
	// Description plugin description
	Description = "Type conversion operations"
	// DefaultPort is the default port to use
	DefaultPort = "10001"
)

// getMessageFromStdin looks through both stdin and the os.Args for a message body to work as input
func getMessageFromStdin() (*message.V1, *message.BodyV1, error) {
	b := &message.BodyV1{}
	m := &message.V1{
		Body: b, // Seed it with a type so marshalling works
	}
	dec := json.NewDecoder(os.Stdin)
	if err := dec.Decode(m); err != nil {
		// Try from os.Args[3] which is where the komand engine code will send the data
		// when it's not over stdin - this will always have a "--" in pos 2
		if len(os.Args) == 4 && os.Args[2] == "--" {
			if err := json.Unmarshal([]byte(os.Args[3]), m); err != nil {
				return nil, nil, err
			}
			return m, b, nil
		}
		// If it wasn't over stdin, and it wasn't in pos3, following "--" in pos2, it's an error
		return nil, nil, fmt.Errorf("Unable to find message in os.Stdin nor os.Args: %+v", os.Args)
	}
	return m, b, nil
}

func main() {
	app := kingpin.New(Name, Description)
	app.Version(Version)

	debug := app.Flag("debug", "Log events to stdout.").Default("false").Bool()
	test := app.Command("test", "Run a test using the start message over STDIN.")
	info := app.Command("info", "Display plugin info (triggers and actions).")
	sample := app.Command("sample", "Show a sample start message for the provided trigger or action.")
	sampleOpt := sample.Arg("handler_name", "Trigger or Action name to generate sample inputs for.").Required().String()
	run := app.Command("run", "Run the plugin (default command). You must supply the start message over STDIN.")
	httpServer := app.Command("http", "Run the plugin as a REST daemon")
	port := app.Flag("port", "The http port to run the server on (defaults to 10001).").Default(DefaultPort).Int()

	args := os.Args[1:] // Chop off the first element, we don't care about the binary name/path

	// Code lifted from old SDK to make kingpin happy
	for i, argv := range args {
		if argv == "--" {
			args = args[0:(i)]
			break
		}
	}
	if len(args) < 1 || (len(args) == 1 && strings.HasSuffix(args[0], "debug")) {
		args = append(args, "run")
	}
	// End of kingpin happiness making

	// Trap the interrupts
	stopChan := make(chan os.Signal, 1)
	signal.Notify(stopChan, syscall.SIGTERM, syscall.SIGINT)
	// Create a context, which will be used to cancel things like triggers in the event of a signal
	ctx, cancel := context.WithCancel(context.Background())
	// Kick off a goroutine to handle the signals and the cancellation
	go cli.HandleShutdown(cancel, stopChan)
	// Invoke the command

	mode := "run"
	cmd, err := app.Parse(args)
	switch kingpin.MustParse(cmd, err) {
	case sample.FullCommand():
		printSampleInput(*sampleOpt)
	case info.FullCommand():
		printInfo()
	case httpServer.FullCommand():
		s, err := http.NewServer(*port, connection.NewCache())
		if err != nil {
			log.Fatal(err)
		}
		fmt.Printf("Plugin type_converter booting HTTP Daemon on port %d...", *port)
		// This will block until the server shuts down
		if err := s.ListenAndServe(); err != nil {
			log.Fatal(err)
		}
	case test.FullCommand():
		mode = "test"
		fallthrough
	case run.FullCommand():
		fallthrough
	default:
		// We're running either an action, or a trigger
		m, b, err := getMessageFromStdin()
		if err != nil {
			log.Fatalf("you must provide a message body via STDIN to run an action or trigger: %s", err.Error())
		}
		switch m.Type {
		case message.ActionStart:
			if err := runOneOffAction(mode, b, *debug); err != nil { // Command should be run, or test
				log.Fatal(err)
			}
		case message.TriggerStart:
			// This will block until the trigger shuts down
			if err := runTriggerDaemon(ctx, mode, b, *debug); err != nil { // Command should be run, or test
				log.Fatal(err)
			}
		default:
			log.Fatalf("unknown message type %s", m.Type)
		}
	}
}

func runOneOffAction(mode string, b *message.BodyV1, isDebug bool) error {
	// Set up all the standard data structures we need to process an action
	cd := &connection.Data{}
	l := plog.NewBufferedLogger(plog.Error)
	if mode == "test" || isDebug {
		l.SetLevel(plog.Info)
	}
	// Actions always default to using STDOUT since we never send dispatch info
	// with their message invocations, but to try and keep things consistent between actions
	// and triggers in the hopes of further generalizing their code, we'll continue
	// to use the method below to make the determination of what to use
	d, err := cli.DispatcherFromRaw(b.Dispatcher, isDebug, mode)
	if err != nil {
		return err
	}
	// Make sure we flush the log out stderr as thats how the engine knows to get it in a one off
	defer l.Flush(os.Stderr)
	response := &message.Response{
		Meta:   []byte(`{}`),
		Status: "ok",
	}
	if err = json.Unmarshal(b.Connection, cd); err != nil {
		return err
	}
	var conn *connection.Connection
	conn, err = connection.Connect(cd, l)
	if err != nil {
		return err
	}
	// Unmarshal the body into the right struct
	// We could avoid the 2 excess json marshal calls in here with a lot of switch-casing on types
	// but in this case, the performance gain is not that great, and it's less generator code to maintain
	// to do it this way.
	// TODO if we suspect this is a bottleneck, profile it and swap back to the switch-case approach
	switch b.Action {
	case "array_to_string":
		input := &actions.ArrayToStringInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating ArrayToStringTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating ArrayToStringTriggerInput. Check logs for details")
		}
		a := &actions.ArrayToStringAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	case "boolean_to_integer":
		input := &actions.BooleanToIntegerInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating BooleanToIntegerTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating BooleanToIntegerTriggerInput. Check logs for details")
		}
		a := &actions.BooleanToIntegerAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	case "boolean_to_string":
		input := &actions.BooleanToStringInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating BooleanToStringTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating BooleanToStringTriggerInput. Check logs for details")
		}
		a := &actions.BooleanToStringAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	case "integer_to_boolean":
		input := &actions.IntegerToBooleanInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating IntegerToBooleanTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating IntegerToBooleanTriggerInput. Check logs for details")
		}
		a := &actions.IntegerToBooleanAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	case "integer_to_number":
		input := &actions.IntegerToNumberInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating IntegerToNumberTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating IntegerToNumberTriggerInput. Check logs for details")
		}
		a := &actions.IntegerToNumberAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	case "integer_to_string":
		input := &actions.IntegerToStringInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating IntegerToStringTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating IntegerToStringTriggerInput. Check logs for details")
		}
		a := &actions.IntegerToStringAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	case "number_to_integer":
		input := &actions.NumberToIntegerInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating NumberToIntegerTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating NumberToIntegerTriggerInput. Check logs for details")
		}
		a := &actions.NumberToIntegerAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	case "object_to_string":
		input := &actions.ObjectToStringInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating ObjectToStringTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating ObjectToStringTriggerInput. Check logs for details")
		}
		a := &actions.ObjectToStringAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	case "string_to_boolean":
		input := &actions.StringToBooleanInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating StringToBooleanTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating StringToBooleanTriggerInput. Check logs for details")
		}
		a := &actions.StringToBooleanAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	case "string_to_float":
		input := &actions.StringToFloatInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating StringToFloatTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating StringToFloatTriggerInput. Check logs for details")
		}
		a := &actions.StringToFloatAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	case "string_to_integer":
		input := &actions.StringToIntegerInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating StringToIntegerTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating StringToIntegerTriggerInput. Check logs for details")
		}
		a := &actions.StringToIntegerAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	case "string_to_list":
		input := &actions.StringToListInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating StringToListTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating StringToListTriggerInput. Check logs for details")
		}
		a := &actions.StringToListAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	case "string_to_object":
		input := &actions.StringToObjectInput{}
		if err = json.Unmarshal(b.Input, input); err != nil {
			return err
		}
		for _, err = range input.Validate(l) {
			log.Println(fmt.Sprintf("Error while validating StringToObjectTriggerInput: %s", err.Error()))
		}
		if err != nil {
			return fmt.Errorf("Error while validating StringToObjectTriggerInput. Check logs for details")
		}
		a := &actions.StringToObjectAction{}
		runMethod := a.Run
		if mode == "test" {
			runMethod = a.Test
		}
		response.Output, err = runMethod(conn, input, l)
	default:
		return fmt.Errorf("unknown action %s", b.Action)
	}
	if err != nil {
		response.Status = "error"
		response.Error = err.Error()
	}
	response.Log = l.String()
	// dispatch the result
	wrapper := &message.V1{
		Body:    response,
		Type:    "action_event",
		Version: "v1",
	}
	d.Send(wrapper)
	return nil
}

func runTriggerDaemon(ctx context.Context, mode string, b *message.BodyV1, isDebug bool) error {
	return nil
}

func printInfo() {
	// General Info / Header
	result := "\n"
	result += fmt.Sprintf("Name:        %s%s%s\n", cli.Green, Name, cli.Reset)
	result += fmt.Sprintf("Vendor:      %s%s%s\n", cli.Green, Vendor, cli.Reset)
	result += fmt.Sprintf("Version:     %s%s%s\n", cli.Green, Version, cli.Reset)
	result += fmt.Sprintf("Description: %s%s%s\n", cli.Green, Description, cli.Reset)

	// Action Info
	result += fmt.Sprintf("\n")
	result += fmt.Sprintf("Actions (%s%d%s): \n", cli.Green, 13, cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "array_to_string", cli.Reset, "Converts an array to a string", cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "boolean_to_integer", cli.Reset, "Converts a boolean to an integer. True -> 1, False -> 0", cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "boolean_to_string", cli.Reset, "Converts a boolean to a string", cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "integer_to_boolean", cli.Reset, "Converts an integer to a boolean. Non-Zero -> True, 0 -> False", cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "integer_to_number", cli.Reset, "Converts an integer to a number", cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "integer_to_string", cli.Reset, "Converts an integer to a string", cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "number_to_integer", cli.Reset, "Converts a number to an integer", cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "object_to_string", cli.Reset, "Encodes an object to a string", cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "string_to_boolean", cli.Reset, "Converts a string to a boolean. Case-insensitive true -> True, else -> False", cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "string_to_float", cli.Reset, "Converts a string to a float", cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "string_to_integer", cli.Reset, "Converts a string to an integer", cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "string_to_list", cli.Reset, "Converts a string to a list of strings", cli.Reset)
	result += fmt.Sprintf("└── %s%s%s (%s%s)\n", cli.Green, "string_to_object", cli.Reset, "Converts a string to an object", cli.Reset)

	fmt.Print(result)
}

func printSampleInput(action string) error {
	connBytes, err := json.Marshal(connection.Data{})
	if err != nil {
		return err
	}
	b := &message.BodyV1{
		Connection: connBytes,
		Meta:       []byte(`{}`),
		Dispatcher: []byte(`{}`),
	}
	m := message.V1{
		Version: "v1",
		Body:    b,
	}

	// 'action' here is just the name of the component (regardless of whether or not it is a trigger or action)
	switch action {
	case "array_to_string":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.ArrayToStringInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	case "boolean_to_integer":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.BooleanToIntegerInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	case "boolean_to_string":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.BooleanToStringInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	case "integer_to_boolean":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.IntegerToBooleanInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	case "integer_to_number":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.IntegerToNumberInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	case "integer_to_string":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.IntegerToStringInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	case "number_to_integer":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.NumberToIntegerInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	case "object_to_string":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.ObjectToStringInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	case "string_to_boolean":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.StringToBooleanInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	case "string_to_float":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.StringToFloatInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	case "string_to_integer":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.StringToIntegerInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	case "string_to_list":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.StringToListInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	case "string_to_object":
		m.Type = "action_start"
		b.Action = action
		actionBytes, err := json.Marshal(actions.StringToObjectInput{})
		if err != nil {
			return err
		}
		b.Input = actionBytes
	default:
		log.Fatalf("Unknown action or trigger %s", action)
	}

	result, err := json.MarshalIndent(&m, " ", "  ")
	if err != nil {
		return err
	}
	fmt.Println(string(result))
	return nil
}
