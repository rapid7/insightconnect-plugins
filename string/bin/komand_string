#!/usr/bin/env python
# GENERATED BY KOMAND SDK - DO NOT EDIT
import komand
from komand_string import connection, actions, triggers


Name = 'String Operations'
Vendor = 'rapid7'
Version = '1.1.0'
Description = 'Provides common programmatic string operations'


class ICONString(komand.Plugin):
    def __init__(self):
        super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
                )
        self.add_action(actions.Lower())

        self.add_action(actions.SetEncoding())

        self.add_action(actions.SplitToList())

        self.add_action(actions.SplitToObject())

        self.add_action(actions.Upper())


def main():
    """Run plugin"""
    cli = komand.CLI(ICONString())
    cli.run()


if __name__ == "__main__":
    main()
